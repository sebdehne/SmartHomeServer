package com.dehnes.smarthome.service.ev_charging_station

class EvChargingManager {

    /*
     * Controls the charging stations:
     * - takes timing into account (EvChargingTimingService)
     * - takes SoC into account (EvSocService)
     * - load sharing: on / off (configurable from webapp)
     */

}